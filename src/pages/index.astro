---
// src/pages/index.astro
import SecHero       from '../components/SecHero.astro';
import SecAbout      from '../components/SecAbout.astro';
import SecServices   from '../components/SecServices.astro';
import SecPortfolio  from '../components/SecPortfolio.astro';
import SecCert       from '../components/SecCert.astro';
import SecHistory    from '../components/SecHistory.astro';
import SecStrength   from '../components/SecStrength.astro';
import SecClients    from '../components/SecClients.astro';
import SecContact    from '../components/SecContact.astro';
---
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>EDS ????癒?뮞????Beyond The Future | ??살춳???⑤벀而??遺억펷??/title>
  <meta name="description" content="獄쎻뫗????쎈뮔?遺우궎?봔??LED ?遺용뮞???쟿??욱돱筌왖. 17?袁⑹벥 野껋?肉??곗쨮 筌ㅼ뮇?????살춳???⑤벀而????블??몃빍??" />
  <link rel="icon" href="/favicon.ico" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <script is:inline src="/i18n/translations.js"></script>
  <link rel="stylesheet" href="/global.css" />
</head>
<body>

  <!-- NAV -->
  <nav class="nav" id="nav">
    <a href="/" class="nav-logo">
      <img class="logo-color" src="/images/logo.png" alt="EDS ????癒?뮞?? />
      <img class="logo-white" src="/images/logo.png" alt="EDS ????癒?뮞?? />
    </a>

    <div class="nav-menu">

      <!-- ???텢???뻣 -->
      <div class="nav-item has-drop">
        <span class="nav-link">???텢???뻣</span>
        <div class="nav-drop">
          <a href="#sec2" class="drop-link" onclick="event.preventDefault();document.getElementById('sec2').scrollIntoView({behavior:'smooth'})">?紐꾧텢筌?/a>
          <a href="#sec6" class="drop-link" onclick="event.preventDefault();document.getElementById('sec6').scrollIntoView({behavior:'smooth'})">?怨좎겫</a>
          <a href="#sec-cert" class="drop-link" onclick="event.preventDefault();document.getElementById('sec-cert').scrollIntoView({behavior:'smooth'})">?紐꾩쵄?袁れ넺</a>
          <a href="#sec7" class="drop-link" onclick="event.preventDefault();document.getElementById('sec7').scrollIntoView({behavior:'smooth'})">??쇰뻻??疫?/a>
        </div>
      </div>

      <!-- ??毓썽겫袁⑸튊 -->
      <div class="nav-item has-drop">
        <span class="nav-link">??毓썽겫袁⑸튊</span>
        <div class="nav-drop">
          <a href="#svc-public" class="drop-link" onclick="event.preventDefault();document.getElementById('svc-public').scrollIntoView({behavior:'smooth'})">?⑤벀?ф묾怨?</a>
          <a href="#svc-edu" class="drop-link" onclick="event.preventDefault();document.getElementById('svc-edu').scrollIntoView({behavior:'smooth'})">?대Ŋ?곫묾怨?</a>
          <a href="#svc-corp" class="drop-link" onclick="event.preventDefault();document.getElementById('svc-corp').scrollIntoView({behavior:'smooth'})">疫꿸퀣毓?/a>
          <a href="#svc-media" class="drop-link" onclick="event.preventDefault();document.getElementById('svc-media').scrollIntoView({behavior:'smooth'})">沃섎챶逾??곴쉽??/a>
          <a href="#svc-religion" class="drop-link" onclick="event.preventDefault();document.getElementById('svc-religion').scrollIntoView({behavior:'smooth'})">?ル굛??/a>
        </div>
      </div>

      <!-- 鈺곌퀡?뽳㎗??源낆쨯??쀫? -->
      <div class="nav-item has-drop">
        <span class="nav-link">鈺곌퀡?뽳㎗??源낆쨯??쀫?</span>
        <div class="nav-drop">
          <a href="#sec5" class="drop-link" onclick="event.preventDefault();document.getElementById('sec5').scrollIntoView({behavior:'smooth'})">沃섎챶逾??而??/a>
          <a href="#sec5" class="drop-link" onclick="event.preventDefault();document.getElementById('sec5').scrollIntoView({behavior:'smooth'})">?뚮똾踰????뺤쒔</a>
          <a href="#sec5" class="drop-link" onclick="event.preventDefault();document.getElementById('sec5').scrollIntoView({behavior:'smooth'})">?遺용뮞???????/a>
          <a href="#sec5" class="drop-link" onclick="event.preventDefault();document.getElementById('sec5').scrollIntoView({behavior:'smooth'})">LED ?袁㏉굝??/a>
        </div>
      </div>

      <!-- ?닌딇뀧??? -->
      <div class="nav-item">
        <a href="#sec4" class="nav-link" onclick="event.preventDefault();document.getElementById('sec4').scrollIntoView({behavior:'smooth'})">?닌딇뀧???</a>
      </div>

      <!-- ?얜챷??-->
      <div class="nav-item">
        <a href="#sec7" class="nav-link" onclick="event.preventDefault();document.getElementById('sec7').scrollIntoView({behavior:'smooth'})">?얜챷??/a>
      </div>

    </div>

    <div class="lang-wrap">
      <button class="lang-btn active" data-lang="ko" onclick="__applyLang('ko')">KO</button>
      <button class="lang-btn" data-lang="en" onclick="__applyLang('en')">EN</button>
      <button class="lang-btn" data-lang="zh" onclick="__applyLang('zh')">ZH</button>
      <button class="lang-btn" data-lang="ja" onclick="__applyLang('ja')">JA</button>
    </div>
  </nav>

  <!-- ?諭???-->
  <SecHero />
  <SecAbout />
  <SecHistory />
  <SecCert />
  <SecServices />
  <SecPortfolio />
  <SecStrength />
  <SecClients />
  <SecContact />

<script is:inline>
  const nav = document.getElementById('nav');

  window.addEventListener('scroll', function() {
    const hero = document.getElementById('sec0');
    const heroHeight = hero ? hero.offsetHeight : 0;
    if (window.scrollY > heroHeight - 60) {
      nav.classList.add('scrolled');
    } else {
      nav.classList.remove('scrolled');
    }
  });

  document.querySelectorAll('a[href^="#"]').forEach(function(a) {
    a.addEventListener('click', function(e) {
      e.preventDefault();
      const target = document.querySelector(a.getAttribute('href'));
      if (target) target.scrollIntoView({ behavior: 'smooth' });
    });
  });

  // ?袁⑸열 fade-up IntersectionObserver
  const fadeObs = new IntersectionObserver(function(entries) {
    entries.forEach(function(e) {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
        fadeObs.unobserve(e.target);
      }
    });
  }, { threshold: 0.08, rootMargin: '0px 0px -40px 0px' });
  document.querySelectorAll('.fade-up').forEach(function(el) { fadeObs.observe(el); });
</script>

<!-- ???? ???쨮???얜챷??甕곌쑵??+ ??밸씜 ???? -->
<div class="float-btn" id="floatBtn" onclick="document.getElementById('inquiryModal').classList.add('open')">
  <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
    <path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
  </svg>
  <span>?얜챷???띾┛</span>
</div>

<!-- ??밸씜 筌뤴뫀??-->
<div class="inq-modal" id="inquiryModal">
  <div class="inq-overlay" onclick="document.getElementById('inquiryModal').classList.remove('open')"></div>
  <div class="inq-box">
    <div class="inq-head">
      <div>
        <div class="inq-title">?袁⑹뿯 ?얜챷??/div>
        <div class="inq-sub">?袁ⓓ??怨룸뼖??????쥓?ㅵ칰??????뺚뵲??덈뼄</div>
      </div>
      <button class="inq-close" onclick="document.getElementById('inquiryModal').classList.remove('open')">??/button>
    </div>

    <div class="inq-body">
      <div class="inq-field">
        <label class="inq-label">???텢筌?<span class="req">*</span></label>
        <input class="inq-input" type="text" placeholder="???텢筌뤿굞????낆젾??곻폒?紐꾩뒄" />
      </div>
      <div class="inq-field">
        <label class="inq-label">???????已?<span class="req">*</span></label>
        <input class="inq-input" type="text" placeholder="??삳쭔?? />
      </div>
      <div class="inq-field">
        <label class="inq-label">?怨뺤뵭筌?<span class="req">*</span></label>
        <div class="inq-tel">
          <input class="inq-input" type="tel" placeholder="010" maxlength="4"/>
          <span>-</span>
          <input class="inq-input" type="tel" placeholder="0000" maxlength="4"/>
          <span>-</span>
          <input class="inq-input" type="tel" placeholder="0000" maxlength="4"/>
        </div>
      </div>
      <div class="inq-field">
        <label class="inq-label">??李??/label>
        <input class="inq-input" type="email" placeholder="example@company.com" />
      </div>
      <div class="inq-field">
        <label class="inq-label">?怨룸뼖 ?브쑴鍮?<span class="req">*</span></label>
        <div class="inq-checks">
          <label class="inq-check"><input type="checkbox" /><span>獄쎻뫗??SI</span></label>
          <label class="inq-check"><input type="checkbox" /><span>??쎈뮔?遺우궎 ?닌딇뀧</span></label>
          <label class="inq-check"><input type="checkbox" /><span>LED ?遺용뮞???쟿??/span></label>
          <label class="inq-check"><input type="checkbox" /><span>??살춳??揶쏅벡???/span></label>
          <label class="inq-check"><input type="checkbox" /><span>?袁⑸춦??????뽯뮞??/span></label>
          <label class="inq-check"><input type="checkbox" /><span>?遺욧맒???벥 ??뽯뮞??/span></label>
          <label class="inq-check"><input type="checkbox" /><span>?醫?癰귣똻??/span></label>
          <label class="inq-check"><input type="checkbox" /><span>疫꿸퀬?</span></label>
        </div>
      </div>
      <div class="inq-field">
        <label class="inq-label">?얜챷????곸뒠</label>
        <textarea class="inq-input inq-textarea" placeholder="?얜챷???뤿뼄 ??곸뒠???癒??嚥?苡???낆젾??곻폒?紐꾩뒄"></textarea>
      </div>
    </div>

    <div class="inq-foot">
      <button class="inq-submit">?얜챷??癰귣?沅→묾???/button>
      <p class="inq-privacy">??깆뵬 09:00 ??18:00 ??곸겫 夷?Tel. 02-857-7744</p>
    </div>
  </div>
</div>

<style>
  
  .float-btn {
    position: fixed; bottom: 32px; right: 32px;
    z-index: 400;
    display: flex; align-items: center; gap: 8px;
    background: #0D3A4A; color: #fff;
    padding: 14px 22px; border-radius: 50px;
    cursor: pointer; font-size: 14px; font-weight: 700;
    box-shadow: 0 4px 24px rgba(13,58,74,0.35);
    transition: background 0.2s, transform 0.2s, box-shadow 0.2s;
    letter-spacing: 0.02em;
  }
  .float-btn:hover {
    background: #00B8D9;
    transform: translateY(-3px);
    box-shadow: 0 8px 32px rgba(0,184,217,0.4);
  }

  
  .inq-modal {
    position: fixed; inset: 0; z-index: 500;
    display: flex; align-items: flex-end; justify-content: flex-end;
    padding: 0 32px 100px;
    pointer-events: none; opacity: 0;
    transition: opacity 0.25s;
  }
  .inq-modal.open { pointer-events: all; opacity: 1; }
  .inq-overlay {
    position: absolute; inset: 0;
    background: rgba(0,0,0,0.4);
    backdrop-filter: blur(4px);
  }
  .inq-box {
    position: relative; z-index: 1;
    background: #fff; border-radius: 16px;
    width: 440px; max-height: 80vh;
    display: flex; flex-direction: column;
    box-shadow: 0 24px 80px rgba(0,0,0,0.2);
    transform: translateY(20px);
    transition: transform 0.3s cubic-bezier(0.16,1,0.3,1);
    overflow: hidden;
  }
  .inq-modal.open .inq-box { transform: translateY(0); }

  
  .inq-head {
    display: flex; align-items: flex-start; justify-content: space-between;
    padding: 24px 28px 20px;
    border-bottom: 1px solid #f0f0f0;
    flex-shrink: 0;
  }
  .inq-title { font-size: 20px; font-weight: 800; color: #0a0a0a; letter-spacing: -0.03em; margin-bottom: 4px; }
  .inq-sub { font-size: 12px; color: #999; }
  .inq-close {
    background: none; border: none; cursor: pointer;
    font-size: 16px; color: #aaa; padding: 4px; line-height: 1;
    transition: color 0.2s;
  }
  .inq-close:hover { color: #0a0a0a; }

  
  .inq-body {
    overflow-y: auto; padding: 20px 28px;
    display: flex; flex-direction: column; gap: 18px;
    flex: 1; min-height: 0;
  }
  .inq-body::-webkit-scrollbar { width: 4px; }
  .inq-body::-webkit-scrollbar-thumb { background: #e0e0e0; border-radius: 2px; }

  
  .inq-field { display: flex; flex-direction: column; gap: 7px; }
  .inq-label { font-size: 12px; font-weight: 700; color: #333; letter-spacing: 0.02em; }
  .req { color: #e53e3e; }
  .inq-input {
    width: 100%; padding: 10px 14px;
    border: 1px solid #e0e0e0; border-radius: 8px;
    font-size: 14px; color: #0a0a0a; font-family: inherit;
    outline: none; transition: border-color 0.2s;
    background: #fafafa;
  }
  .inq-input:focus { border-color: #00B8D9; background: #fff; }
  .inq-input::placeholder { color: #bbb; }

  
  .inq-tel { display: flex; align-items: center; gap: 6px; }
  .inq-tel span { color: #aaa; font-weight: 500; }
  .inq-tel .inq-input { text-align: center; }

  
  .inq-textarea { resize: none; height: 90px; }

  
  .inq-checks { display: flex; flex-wrap: wrap; gap: 8px; }
  .inq-check {
    display: flex; align-items: center; gap: 6px;
    font-size: 13px; color: #555; cursor: pointer;
    padding: 7px 14px;
    border: 1px solid #e0e0e0; border-radius: 20px;
    transition: all 0.15s;
  }
  .inq-check:hover { border-color: #00B8D9; color: #00B8D9; }
  .inq-check input { display: none; }
  .inq-check input:checked + span { color: #0D3A4A; font-weight: 700; }
  .inq-check:has(input:checked) { border-color: #0D3A4A; background: rgba(13,58,74,0.05); }

  
  .inq-foot {
    padding: 16px 28px 24px;
    border-top: 1px solid #f0f0f0;
    flex-shrink: 0;
  }
  .inq-submit {
    width: 100%; padding: 14px;
    background: #0D3A4A; color: #fff;
    border: none; border-radius: 8px;
    font-size: 14px; font-weight: 700;
    cursor: pointer; letter-spacing: 0.06em;
    font-family: inherit;
    transition: background 0.2s;
    margin-bottom: 10px;
  }
  .inq-submit:hover { background: #00B8D9; }
  .inq-privacy { font-size: 11px; color: #bbb; text-align: center; }

  @media (max-width: 480px) {
    .float-btn span { display: none; }
    .float-btn { padding: 14px; border-radius: 50%; }
    .inq-modal { padding: 0 0 90px; align-items: flex-end; justify-content: center; }
    .inq-box { width: 100%; border-radius: 16px 16px 0 0; max-height: 90vh; }
  }
</style>
</body>
</html>

